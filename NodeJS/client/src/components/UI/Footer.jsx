import styled from "styled-components";

const StyledFooter = styled.footer`
  height: 80px;
`

const Footer = () => {
  return (
    <StyledFooter>
      footer
    </StyledFooter>
  );
}

export default Footer;